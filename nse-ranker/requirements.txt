# Core service: universe, data, corporate actions, neutralisation, ranking,
# backtest, report. Everything the test suite exercises.
#
# numpy is the ONLY third-party dependency here, on purpose. The pure-math
# modules must import and be testable with no model stack present, so anything
# heavier belongs in requirements-kronos.txt.
numpy>=1.26
pytest>=8.0
