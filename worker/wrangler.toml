name = "xerxes-stock-refresh"
main = "index.js"
compatibility_date = "2024-11-01"

# Non-secret config. Adjust CODE_BRANCH / ALLOW_ORIGIN to your fork if needed.
[vars]
REPO = "JazzeshWolf/xerxes"
CODE_BRANCH = "claude/nifty-option-screener-93xv0y"
DATA_BRANCH = "stocks-data"
ALLOW_ORIGIN = "https://jazzeshwolf.github.io"

# GH_PAT is a SECRET — never put it here. Set it with:
#   wrangler secret put GH_PAT
